{
    "apps": [
        {
            "name": "EpicureBackend",
            "script": "./server.ts",
            "env": {
                "NODE_ENV": "production"
            }
        }
    ],
    "deploy": {
        "production": {
            "key": "/Users/shaharsaadon/.ssh/secret_epicure.pem",
            "user": "ubuntu",
            "host": ["ec2-13-49-244-35.eu-north-1.compute.amazonaws.com"],
            "ref": "origin/jwtAuthentication",
            "repo": "git@github.com:ShaharSaadon/epicure-backend",
            "path": "/home/ubuntu/epicure/backend",
            "ssh_options": "StrictHostKeyChecking=no",
            "post-deploy": "mkdir -p logs && npm i && pm2 reload ecosystem.json --env production",
            "pre-deploy-local": "echo 'Deploying backend code to server'",
            "env": {
                "NODE_ENV": "production"
            }
        }
    }
}
